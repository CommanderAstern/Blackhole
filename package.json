{
  "name": "moonio",
  "version": "1.0.0",
  "description": "A standalone black hole simulation built with plain HTML, CSS, and WebGL2.",
  "main": "main.js",
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "deploy": "npm run build && npx wrangler pages deploy --commit-dirty=true",
    "preview": "npm run build && npx wrangler pages dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "playwright": "^1.58.2"
  }
}
